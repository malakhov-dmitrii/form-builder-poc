import { FormItemConfig } from '../interfaces/FormItem';

export const defaultConfig: FormItemConfig = {
  name: 'Default name',
  disabled: false,
  placeholder: 'Default placeholder',
  checked: true,
  label: 'Default label',
  required: false,
};

export const a = '';
